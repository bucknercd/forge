[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "forge"
version = "0.1.0"
description = "Forge v1"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
